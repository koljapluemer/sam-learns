# have a global shared page shell for info (=home) stats and settings and have the apps provide a component to insert, instead of the other way around

...this hopefully prevents the massive and random divergences with wrapper div and arbitrary max-w in every app's pages